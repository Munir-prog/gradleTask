plugins {
    id "com.palantir.docker" version "0.22.1"
    id "com.palantir.docker-run" version "0.22.1"
}

version '0.1.0'

docker {
    name "${project.name}:${project.version}"
    files 'someProgram.jar'
}

dockerRun {
    name "${project.name}"
    image "${project.name}:${project.version}"
    ports '16262:16262'
    clean true
}
